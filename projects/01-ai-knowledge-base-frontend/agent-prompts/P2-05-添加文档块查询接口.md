# Agent任务：添加文档块查询接口

**优先级：** P2 - 提升透明度  
**预计工作量：** 3小时  
**影响范围：** 文档管理透明度

---

## 任务背景

### 问题描述
文档上传后会被切分成多个文档块(chunks)进行向量化存储，但用户无法查看切块结果。这降低了系统的透明度，用户不知道文档如何被处理。

### 使用场景
1. **调试** - 验证文档切块是否合理
2. **优化** - 根据切块结果调整切块策略
3. **透明度** - 让用户了解文档处理过程
4. **质量控制** - 检查切块质量

### 数据已存在
- ✅ 数据库表：`document_chunks`
- ✅ Repository方法存在（需要添加）
- ❌ HTTP接口不存在
- ❌ 前端展示不存在

---

## 实现目标

添加文档块查询功能：
1. 后端接口：`GET /api/v1/documents/:id/chunks`
2. 前端展示：文档预览对话框中显示切块列表
3. 支持分页查询
4. 显示切块统计信息

---

## 实现步骤

### 第一部分：后端实现

#### 步骤1：在ChunkRepository添加查询方法

**文件：** `projects/01-ai-knowledge-base/internal/repository/chunk_repository.go`

**在文件末尾添加：**

```go
// ListChunksByDocument lists all chunks for a document with pagination
func (r *ChunkRepository) ListChunksByDocument(ctx context.Context, documentID string, limit, offset int) ([]*models.DocumentChunk, int64, error) {
	var chunks []*models.DocumentChunk
	var total int64

	// Get total count
	if err := r.db.WithContext(ctx).
		Model(&models.DocumentChunk{}).
		Where("document_id = ? AND deleted_at IS NULL", documentID).
		Count(&total).Error; err != nil {
		return nil, 0, err
	}

	// Get chunks with pagination
	if err := r.db.WithContext(ctx).
		Where("document_id = ? AND deleted_at IS NULL", documentID).
		Order("chunk_index ASC").
		Limit(limit).
		Offset(offset).
		Find(&chunks).Error; err != nil {
		return nil, 0, err
	}

	return chunks, total, nil
}

// GetChunkStats returns statistics for a document's chunks
func (r *ChunkRepository) GetChunkStats(ctx context.Context, documentID string) (*ChunkStats, error) {
	var stats ChunkStats

	err := r.db.WithContext(ctx).
		Model(&models.DocumentChunk{}).
		Select(`
			COUNT(*) as total_chunks,
			AVG(LENGTH(content)) as avg_chunk_size,
			MIN(LENGTH(content)) as min_chunk_size,
			MAX(LENGTH(content)) as max_chunk_size,
			SUM(token_count) as total_tokens
		`).
		Where("document_id = ? AND deleted_at IS NULL", documentID).
		Scan(&stats).Error

	return &stats, err
}

// ChunkStats represents chunk statistics
type ChunkStats struct {
	TotalChunks   int64   `json:"total_chunks"`
	AvgChunkSize  float64 `json:"avg_chunk_size"`
	MinChunkSize  int64   `json:"min_chunk_size"`
	MaxChunkSize  int64   `json:"max_chunk_size"`
	TotalTokens   int64   `json:"total_tokens"`
}
```

#### 步骤2：创建DocumentHandler方法

**文件：** `projects/01-ai-knowledge-base/internal/handler/document_handler.go`

**在文件末尾添加：**

```go
// ListDocumentChunks lists all chunks for a document
// @Summary List document chunks
// @Description Get all chunks for a document with pagination
// @Tags documents
// @Produce json
// @Param id path string true "Document ID"
// @Param limit query int false "Number of results per page" default(50)
// @Param offset query int false "Offset for pagination" default(0)
// @Security BearerAuth
// @Success 200 {object} middleware.SuccessResponse{data=DocumentChunksResponse}
// @Failure 400 {object} middleware.ErrorResponse
// @Failure 404 {object} middleware.ErrorResponse
// @Failure 500 {object} middleware.ErrorResponse
// @Router /documents/{id}/chunks [get]
func (h *DocumentHandler) ListDocumentChunks(c *gin.Context) {
	docID := c.Param("id")

	// Parse pagination
	limit, _ := strconv.Atoi(c.DefaultQuery("limit", "50"))
	offset, _ := strconv.Atoi(c.DefaultQuery("offset", "0"))

	// Validate limits
	if limit <= 0 || limit > 100 {
		limit = 50
	}
	if offset < 0 {
		offset = 0
	}

	// Get document (verify it exists and user has access)
	doc, err := h.docService.GetDocument(c.Request.Context(), docID)
	if err != nil {
		if err == repository.ErrDocumentNotFound {
			middleware.RespondNotFound(c, "document not found")
			return
		}
		middleware.RespondInternalError(c, "failed to get document: "+err.Error())
		return
	}

	// Get chunks
	chunks, total, err := h.docService.ListDocumentChunks(c.Request.Context(), docID, limit, offset)
	if err != nil {
		middleware.RespondInternalError(c, "failed to list chunks: "+err.Error())
		return
	}

	// Get chunk statistics
	stats, _ := h.docService.GetChunkStats(c.Request.Context(), docID)

	response := DocumentChunksResponse{
		DocumentID:   doc.ID,
		DocumentName: doc.Filename,
		Chunks:       chunks,
		Total:        total,
		Limit:        limit,
		Offset:       offset,
		Stats:        stats,
	}

	middleware.RespondWithSuccess(c, response)
}

// DocumentChunksResponse represents chunks list response
type DocumentChunksResponse struct {
	DocumentID   string                  `json:"document_id"`
	DocumentName string                  `json:"document_name"`
	Chunks       []*models.DocumentChunk `json:"chunks"`
	Total        int64                   `json:"total"`
	Limit        int                     `json:"limit"`
	Offset       int                     `json:"offset"`
	Stats        *repository.ChunkStats  `json:"stats,omitempty"`
}
```

#### 步骤3：在DocumentService添加方法

**文件：** `projects/01-ai-knowledge-base/internal/service/document_service.go`

**在service末尾添加：**

```go
// ListDocumentChunks lists all chunks for a document
func (s *DocumentService) ListDocumentChunks(ctx context.Context, documentID string, limit, offset int) ([]*models.DocumentChunk, int64, error) {
	return s.chunkRepo.ListChunksByDocument(ctx, documentID, limit, offset)
}

// GetChunkStats returns chunk statistics for a document
func (s *DocumentService) GetChunkStats(ctx context.Context, documentID string) (*repository.ChunkStats, error) {
	return s.chunkRepo.GetChunkStats(ctx, documentID)
}
```

**注意：** 需要先在DocumentService结构体中添加chunkRepo字段：

```go
type DocumentService struct {
	docRepo     *repository.DocumentRepository
	chunkRepo   *repository.ChunkRepository  // 添加这一行
	kbService   *KBService
	processor   *DocumentProcessor
	// ... 其他字段
}
```

#### 步骤4：注册路由

**文件：** `projects/01-ai-knowledge-base/cmd/server/main.go`

**在documents路由组中添加（约第274行之后）：**

```go
docs.POST("/batch-delete", docHandler.BatchDeleteDocuments)
docs.GET("/:id/download", docHandler.DownloadDocument)
docs.GET("/:id/preview", docHandler.PreviewDocument)
// ====== 新增 ======
docs.GET("/:id/chunks", docHandler.ListDocumentChunks)
// =================
```

#### 步骤5：修改DocumentService初始化

**文件：** `projects/01-ai-knowledge-base/cmd/server/main.go`

**修改DocumentService初始化（约第199行）：**

```go
// 原来的代码
docService := service.NewDocumentService(docRepo, kbService, docProcessor, "./uploads", 100*1024*1024, 1000, 200)

// 修改为（添加chunkRepo参数）
docService := service.NewDocumentService(docRepo, chunkRepo, kbService, docProcessor, "./uploads", 100*1024*1024, 1000, 200)
```

**同时修改 NewDocumentService 函数签名：**

**文件：** `projects/01-ai-knowledge-base/internal/service/document_service.go`

```go
// NewDocumentService creates a new document service
func NewDocumentService(
	docRepo *repository.DocumentRepository,
	chunkRepo *repository.ChunkRepository,  // 添加这个参数
	kbService *KBService,
	processor *DocumentProcessor,
	uploadDir string,
	maxFileSize int64,
	chunkSize int,
	overlapSize int,
) *DocumentService {
	return &DocumentService{
		docRepo:     docRepo,
		chunkRepo:   chunkRepo,  // 添加这一行
		kbService:   kbService,
		processor:   processor,
		uploadDir:   uploadDir,
		maxFileSize: maxFileSize,
		chunkSize:   chunkSize,
		overlapSize: overlapSize,
	}
}
```

---

### 第二部分：前端实现

#### 步骤6：添加类型定义

**文件：** `projects/01-ai-knowledge-base-frontend/src/types/index.ts`

**在Chunk Types区域添加：**

```typescript
export interface ChunkStats {
  total_chunks: number
  avg_chunk_size: number
  min_chunk_size: number
  max_chunk_size: number
  total_tokens: number
}

export interface DocumentChunksResponse {
  document_id: string
  document_name: string
  chunks: Chunk[]
  total: number
  limit: number
  offset: number
  stats?: ChunkStats
}
```

#### 步骤7：添加服务方法

**文件：** `projects/01-ai-knowledge-base-frontend/src/services/documentService.ts`

**添加方法：**

```typescript
/**
 * List document chunks
 */
async listDocumentChunks(
  documentId: string, 
  limit: number = 50, 
  offset: number = 0
): Promise<DocumentChunksResponse> {
  const response = await api.get<APIResponse<DocumentChunksResponse>>(
    `/documents/${documentId}/chunks`,
    { params: { limit, offset } }
  )
  return response.data.data!
}
```

#### 步骤8：添加Hook

**文件：** `projects/01-ai-knowledge-base-frontend/src/hooks/useDocuments.ts`

**添加Hook：**

```typescript
/**
 * Hook to fetch document chunks
 */
export function useDocumentChunks(documentId: string | null, limit: number = 50) {
  return useQuery({
    queryKey: ['document-chunks', documentId, limit],
    queryFn: () => documentService.listDocumentChunks(documentId!, limit, 0),
    enabled: !!documentId,
    staleTime: 5 * 60 * 1000,
  })
}
```

#### 步骤9：修改DocumentPreviewDialog组件

**文件：** `projects/01-ai-knowledge-base-frontend/src/components/DocumentPreviewDialog.tsx`

**添加chunks展示：**

```typescript
import { useDocumentChunks } from '@/hooks/useDocuments'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'

// 在组件中添加
const { data: chunksData } = useDocumentChunks(document?.id || null, 50)

// 在内容预览部分改用Tabs
<Tabs defaultValue="content" className="w-full">
  <TabsList className="grid w-full grid-cols-2">
    <TabsTrigger value="content">文本内容</TabsTrigger>
    <TabsTrigger value="chunks">
      文档块 ({chunksData?.total || 0})
    </TabsTrigger>
  </TabsList>

  <TabsContent value="content">
    <ScrollArea className="h-96 border rounded-lg p-4">
      {preview.content ? (
        <pre className="text-sm whitespace-pre-wrap font-mono">
          {preview.content}
        </pre>
      ) : (
        <p className="text-muted-foreground text-center py-8">
          该文档未包含可提取的文本内容
        </p>
      )}
    </ScrollArea>
  </TabsContent>

  <TabsContent value="chunks">
    {chunksData ? (
      <div className="space-y-4">
        {/* 统计信息 */}
        {chunksData.stats && (
          <div className="grid grid-cols-4 gap-4 p-4 bg-muted rounded-lg text-sm">
            <div>
              <p className="text-muted-foreground">总块数</p>
              <p className="font-semibold">{chunksData.stats.total_chunks}</p>
            </div>
            <div>
              <p className="text-muted-foreground">平均大小</p>
              <p className="font-semibold">
                {Math.round(chunksData.stats.avg_chunk_size)} 字符
              </p>
            </div>
            <div>
              <p className="text-muted-foreground">大小范围</p>
              <p className="font-semibold">
                {chunksData.stats.min_chunk_size} - {chunksData.stats.max_chunk_size}
              </p>
            </div>
            <div>
              <p className="text-muted-foreground">总Token数</p>
              <p className="font-semibold">{chunksData.stats.total_tokens}</p>
            </div>
          </div>
        )}

        {/* 块列表 */}
        <ScrollArea className="h-96 border rounded-lg">
          <div className="space-y-2 p-4">
            {chunksData.chunks.map((chunk) => (
              <div
                key={chunk.id}
                className="p-3 border rounded-lg hover:bg-muted/50 transition-colors"
              >
                <div className="flex items-center justify-between mb-2">
                  <Badge variant="outline">块 #{chunk.chunk_index}</Badge>
                  <span className="text-xs text-muted-foreground">
                    {chunk.content.length} 字符
                  </span>
                </div>
                <p className="text-sm text-muted-foreground line-clamp-3">
                  {chunk.content}
                </p>
              </div>
            ))}
          </div>
        </ScrollArea>

        {/* 分页 */}
        {chunksData.total > 50 && (
          <div className="text-center text-sm text-muted-foreground">
            显示 {chunksData.chunks.length} / {chunksData.total} 个文档块
          </div>
        )}
      </div>
    ) : (
      <div className="text-center py-8 text-muted-foreground">
        加载中...
      </div>
    )}
  </TabsContent>
</Tabs>
```

---

## 验证方法

### 后端测试

```bash
TOKEN="your_jwt_token"

# 1. 上传并处理一个文档
curl -X POST http://localhost:8080/api/v1/documents \
  -H "Authorization: Bearer $TOKEN" \
  -F "kb_id=kb_xxx" \
  -F "file=@test.pdf"

# 等待文档处理完成

# 2. 获取文档块列表
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/documents/doc_xxx/chunks

# 预期响应：包含chunks数组和stats统计信息

# 3. 测试分页
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:8080/api/v1/documents/doc_xxx/chunks?limit=10&offset=0"
```

### 前端测试

1. 访问文档管理页面
2. 点击文档的"眼睛"图标
3. 在预览对话框中切换到"文档块"标签
4. 验证：
   - [ ] 显示统计信息（总块数、平均大小等）
   - [ ] 显示块列表
   - [ ] 每个块显示索引和内容预览
   - [ ] 内容可滚动查看

---

## 注意事项

### 1. 性能考虑
- 限制单次查询的块数量（最多100个）
- 对于大文档，使用分页
- 考虑添加缓存

### 2. 用户体验
- 提供搜索功能（在块中搜索关键词）
- 支持复制单个块的内容
- 显示向量化状态

### 3. 后续增强
```typescript
// 添加块搜索
searchInChunks(documentId, keyword)

// 添加块详情查看
getChunkDetail(chunkId) // 包含向量信息

// 添加块重新生成
regenerateChunk(chunkId)
```

---

## 完成标准

- [ ] ChunkRepository添加查询方法
- [ ] DocumentHandler添加chunks接口
- [ ] DocumentService添加方法
- [ ] 路由注册完成
- [ ] 前端类型定义完成
- [ ] 前端服务方法完成
- [ ] DocumentPreviewDialog集成完成
- [ ] 后端测试通过
- [ ] 前端展示正常
- [ ] 代码审查通过

---

## 涉及文件清单

**后端修改：**
- `internal/repository/chunk_repository.go`
- `internal/handler/document_handler.go`
- `internal/service/document_service.go`
- `cmd/server/main.go`

**前端修改：**
- `src/types/index.ts`
- `src/services/documentService.ts`
- `src/hooks/useDocuments.ts`
- `src/components/DocumentPreviewDialog.tsx`

---

**任务完成后，用户可以查看文档的切块详情，提升系统透明度。**

