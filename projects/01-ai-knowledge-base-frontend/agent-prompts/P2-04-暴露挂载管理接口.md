# Agent任务：暴露挂载管理接口

**优先级：** P2 - 架构优化  
**预计工作量：** 6小时  
**影响范围：** 权限管理体系

---

## 任务背景

### 问题描述
后端Repository层已实现挂载记录查询和权限管理功能，但Handler层未暴露HTTP接口，前端无法调用。这导致无法实现权限管理页面和审计功能。

### 现有能力（Repository层）
```go
// internal/repository/mount_repository.go
ListMountsForKB(ctx, kbID)                  // 查询知识库的所有挂载
ListMountsForTenant(ctx, tenantID)          // 查询租户的所有挂载  
ListMountsForOrganization(ctx, organizationID) // 查询组织的所有挂载
ListMountsForUser(ctx, userID)              // 查询用户的所有挂载
UpdateMountPermissions(ctx, mountID, permissions) // 更新挂载权限
GetMount(ctx, mountID)                      // 获取单个挂载记录
```

### 使用场景
1. **权限管理页面** - 查看谁有权访问某个知识库
2. **用户权限审计** - 查看用户有权访问哪些知识库
3. **组织管理** - 管理组织级别的知识库访问权限
4. **审计日志** - 记录权限变更历史

---

## 实现目标

暴露5个HTTP接口：
1. `GET /api/v1/knowledge-bases/:id/mounts` - 获取知识库的挂载列表
2. `GET /api/v1/mounts` - 获取挂载列表（支持过滤）
3. `GET /api/v1/mounts/:id` - 获取单个挂载详情
4. `PUT /api/v1/mounts/:id/permissions` - 更新挂载权限
5. `GET /api/v1/user/mounts` - 获取当前用户的所有挂载

---

## 实现步骤

### 步骤1：在KBHandler中添加挂载查询方法

**文件：** `projects/01-ai-knowledge-base/internal/handler/kb_handler.go`

**在文件末尾添加：**

```go
// ListKBMounts godoc
// @Summary 获取知识库的挂载列表
// @Description 获取指定知识库的所有挂载记录，包括租户、组织和用户级别的挂载
// @Tags 知识库挂载
// @Produce json
// @Param id path string true "知识库ID"
// @Success 200 {object} middleware.SuccessResponse{data=object{mounts=[]models.KnowledgeBaseMount,total=int}} "挂载列表"
// @Failure 400 {object} middleware.ErrorResponse "知识库ID必填"
// @Failure 401 {object} middleware.ErrorResponse "未授权"
// @Failure 404 {object} middleware.ErrorResponse "知识库不存在"
// @Failure 500 {object} middleware.ErrorResponse "服务器内部错误"
// @Security BearerAuth
// @Router /knowledge-bases/{id}/mounts [get]
func (h *KBHandler) ListKBMounts(c *gin.Context) {
	kbID := c.Param("id")
	if kbID == "" {
		middleware.RespondBadRequest(c, "KB ID is required")
		return
	}

	// 验证知识库是否存在
	_, err := h.kbService.GetKB(c.Request.Context(), kbID)
	if err != nil {
		if err == repository.ErrKBNotFound {
			middleware.RespondNotFound(c, "Knowledge base not found")
			return
		}
		middleware.RespondInternalError(c, "Failed to get knowledge base: "+err.Error())
		return
	}

	// 获取挂载列表
	mounts, err := h.kbService.ListKBMounts(c.Request.Context(), kbID)
	if err != nil {
		middleware.RespondInternalError(c, "Failed to list mounts: "+err.Error())
		return
	}

	middleware.RespondWithSuccess(c, gin.H{
		"mounts": mounts,
		"total":  len(mounts),
	})
}

// ListMounts godoc
// @Summary 获取挂载列表
// @Description 获取挂载列表，支持按类型过滤（tenant/organization/user）
// @Tags 知识库挂载
// @Produce json
// @Param mount_type query string false "挂载类型：tenant/organization/user"
// @Param tenant_id query string false "租户ID过滤"
// @Param organization_id query string false "组织ID过滤"
// @Param user_id query string false "用户ID过滤"
// @Success 200 {object} middleware.SuccessResponse{data=object{mounts=[]models.KnowledgeBaseMount,total=int}} "挂载列表"
// @Failure 401 {object} middleware.ErrorResponse "未授权"
// @Failure 500 {object} middleware.ErrorResponse "服务器内部错误"
// @Security BearerAuth
// @Router /mounts [get]
func (h *KBHandler) ListMounts(c *gin.Context) {
	mountType := c.Query("mount_type")
	tenantID := c.Query("tenant_id")
	organizationID := c.Query("organization_id")
	userID := c.Query("user_id")

	// 如果没有提供任何过滤条件，使用当前用户的上下文
	user := middleware.MustGetUserContext(c)
	if tenantID == "" && organizationID == "" && userID == "" {
		tenantID = user.TenantID
	}

	var mounts []*models.KnowledgeBaseMount
	var err error

	// 根据过滤条件查询
	switch mountType {
	case "tenant":
		if tenantID == "" {
			tenantID = user.TenantID
		}
		mounts, err = h.kbService.ListMountsForTenant(c.Request.Context(), tenantID)
	case "organization":
		if organizationID == "" {
			organizationID = user.OrganizationID
		}
		mounts, err = h.kbService.ListMountsForOrganization(c.Request.Context(), organizationID)
	case "user":
		if userID == "" {
			userID = user.ID
		}
		mounts, err = h.kbService.ListMountsForUser(c.Request.Context(), userID)
	default:
		// 返回当前用户可见的所有挂载
		mounts, err = h.kbService.ListMountsForTenant(c.Request.Context(), tenantID)
	}

	if err != nil {
		middleware.RespondInternalError(c, "Failed to list mounts: "+err.Error())
		return
	}

	middleware.RespondWithSuccess(c, gin.H{
		"mounts": mounts,
		"total":  len(mounts),
		"filter": gin.H{
			"mount_type":      mountType,
			"tenant_id":       tenantID,
			"organization_id": organizationID,
			"user_id":         userID,
		},
	})
}

// GetMount godoc
// @Summary 获取挂载详情
// @Description 获取单个挂载记录的详细信息
// @Tags 知识库挂载
// @Produce json
// @Param id path int true "挂载ID"
// @Success 200 {object} middleware.SuccessResponse{data=models.KnowledgeBaseMount} "挂载详情"
// @Failure 400 {object} middleware.ErrorResponse "挂载ID无效"
// @Failure 401 {object} middleware.ErrorResponse "未授权"
// @Failure 404 {object} middleware.ErrorResponse "挂载不存在"
// @Failure 500 {object} middleware.ErrorResponse "服务器内部错误"
// @Security BearerAuth
// @Router /mounts/{id} [get]
func (h *KBHandler) GetMount(c *gin.Context) {
	mountIDStr := c.Param("id")
	if mountIDStr == "" {
		middleware.RespondBadRequest(c, "Mount ID is required")
		return
	}

	mountID, err := strconv.ParseInt(mountIDStr, 10, 64)
	if err != nil {
		middleware.RespondBadRequest(c, "Invalid mount ID")
		return
	}

	mount, err := h.kbService.GetMount(c.Request.Context(), mountID)
	if err != nil {
		if err == repository.ErrMountNotFound {
			middleware.RespondNotFound(c, "Mount not found")
			return
		}
		middleware.RespondInternalError(c, "Failed to get mount: "+err.Error())
		return
	}

	middleware.RespondWithSuccess(c, mount)
}

// UpdateMountPermissions godoc
// @Summary 更新挂载权限
// @Description 更新指定挂载的权限设置
// @Tags 知识库挂载
// @Accept json
// @Produce json
// @Param id path int true "挂载ID"
// @Param request body UpdateMountPermissionsRequest true "权限更新请求"
// @Success 200 {object} middleware.SuccessResponse{data=object{message=string}} "权限更新成功"
// @Failure 400 {object} middleware.ErrorResponse "请求参数错误"
// @Failure 401 {object} middleware.ErrorResponse "未授权"
// @Failure 404 {object} middleware.ErrorResponse "挂载不存在"
// @Failure 500 {object} middleware.ErrorResponse "服务器内部错误"
// @Security BearerAuth
// @Router /mounts/{id}/permissions [put]
func (h *KBHandler) UpdateMountPermissions(c *gin.Context) {
	mountIDStr := c.Param("id")
	if mountIDStr == "" {
		middleware.RespondBadRequest(c, "Mount ID is required")
		return
	}

	mountID, err := strconv.ParseInt(mountIDStr, 10, 64)
	if err != nil {
		middleware.RespondBadRequest(c, "Invalid mount ID")
		return
	}

	var req UpdateMountPermissionsRequest
	if err := c.ShouldBindJSON(&req); err != nil {
		middleware.RespondBadRequest(c, "Invalid request: "+err.Error())
		return
	}

	// 验证权限格式
	validPermissions := []string{"can_read", "can_write", "can_delete", "can_share"}
	for key := range req.Permissions {
		valid := false
		for _, validPerm := range validPermissions {
			if key == validPerm {
				valid = true
				break
			}
		}
		if !valid {
			middleware.RespondBadRequest(c, "Invalid permission key: "+key)
			return
		}
	}

	if err := h.kbService.UpdateMountPermissions(c.Request.Context(), mountID, req.Permissions); err != nil {
		if err == repository.ErrMountNotFound {
			middleware.RespondNotFound(c, "Mount not found")
			return
		}
		middleware.RespondInternalError(c, "Failed to update permissions: "+err.Error())
		return
	}

	middleware.RespondWithSuccess(c, gin.H{
		"message": "Permissions updated successfully",
	})
}

// GetUserMounts godoc
// @Summary 获取当前用户的所有挂载
// @Description 获取当前用户通过租户、组织和个人级别可访问的所有知识库挂载
// @Tags 用户相关
// @Produce json
// @Success 200 {object} middleware.SuccessResponse{data=object{mounts=[]models.KnowledgeBaseMount,total=int,breakdown=object}} "用户挂载列表"
// @Failure 401 {object} middleware.ErrorResponse "未授权"
// @Failure 500 {object} middleware.ErrorResponse "服务器内部错误"
// @Security BearerAuth
// @Router /user/mounts [get]
func (h *KBHandler) GetUserMounts(c *gin.Context) {
	user := middleware.MustGetUserContext(c)

	// 获取三级挂载
	tenantMounts, _ := h.kbService.ListMountsForTenant(c.Request.Context(), user.TenantID)
	orgMounts, _ := h.kbService.ListMountsForOrganization(c.Request.Context(), user.OrganizationID)
	userMounts, _ := h.kbService.ListMountsForUser(c.Request.Context(), user.ID)

	// 合并所有挂载（去重）
	mountMap := make(map[string]*models.KnowledgeBaseMount)
	for _, m := range tenantMounts {
		mountMap[m.KBID] = m
	}
	for _, m := range orgMounts {
		mountMap[m.KBID] = m
	}
	for _, m := range userMounts {
		mountMap[m.KBID] = m
	}

	allMounts := make([]*models.KnowledgeBaseMount, 0, len(mountMap))
	for _, m := range mountMap {
		allMounts = append(allMounts, m)
	}

	middleware.RespondWithSuccess(c, gin.H{
		"mounts": allMounts,
		"total":  len(allMounts),
		"breakdown": gin.H{
			"tenant_mounts":       len(tenantMounts),
			"organization_mounts": len(orgMounts),
			"user_mounts":         len(userMounts),
		},
	})
}

// Request/Response DTOs

type UpdateMountPermissionsRequest struct {
	Permissions models.JSONMap `json:"permissions" binding:"required"`
}
```

### 步骤2：在KBService中添加方法

**文件：** `projects/01-ai-knowledge-base/internal/service/kb_service.go`

**在service末尾添加：**

```go
// ListKBMounts lists all mounts for a knowledge base
func (s *KBService) ListKBMounts(ctx context.Context, kbID string) ([]*models.KnowledgeBaseMount, error) {
	return s.mountRepo.ListMountsForKB(ctx, kbID)
}

// ListMountsForTenant lists tenant-level mounts
func (s *KBService) ListMountsForTenant(ctx context.Context, tenantID string) ([]*models.KnowledgeBaseMount, error) {
	return s.mountRepo.ListMountsForTenant(ctx, tenantID)
}

// ListMountsForOrganization lists organization-level mounts
func (s *KBService) ListMountsForOrganization(ctx context.Context, organizationID string) ([]*models.KnowledgeBaseMount, error) {
	return s.mountRepo.ListMountsForOrganization(ctx, organizationID)
}

// ListMountsForUser lists user-level mounts
func (s *KBService) ListMountsForUser(ctx context.Context, userID string) ([]*models.KnowledgeBaseMount, error) {
	return s.mountRepo.ListMountsForUser(ctx, userID)
}

// GetMount gets a mount by ID
func (s *KBService) GetMount(ctx context.Context, mountID int64) (*models.KnowledgeBaseMount, error) {
	return s.mountRepo.GetMount(ctx, mountID)
}

// UpdateMountPermissions updates mount permissions
func (s *KBService) UpdateMountPermissions(ctx context.Context, mountID int64, permissions models.JSONMap) error {
	return s.mountRepo.UpdateMountPermissions(ctx, mountID, permissions)
}
```

### 步骤3：注册路由

**文件：** `projects/01-ai-knowledge-base/cmd/server/main.go`

**修改路由配置（约第244-261行）：**

```go
// Knowledge Bases
kbs := authenticated.Group("/knowledge-bases")
{
	kbs.GET("", kbHandler.ListKBs)
	kbs.POST("", kbHandler.CreateKB)
	kbs.GET("/:id", kbHandler.GetKB)
	kbs.PUT("/:id", kbHandler.UpdateKB)
	kbs.DELETE("/:id", kbHandler.DeleteKB)
	kbs.GET("/:id/stats", kbHandler.GetKBStats)
	// ====== 新增 ======
	kbs.GET("/:id/mounts", kbHandler.ListKBMounts)
	// =================
}

// Mounts
mounts := authenticated.Group("/mounts")
{
	mounts.POST("/tenant", kbHandler.MountKBToTenant)
	mounts.POST("/organization", kbHandler.MountKBToOrganization)
	mounts.POST("/user", kbHandler.MountKBToUser)
	mounts.DELETE("/:id", kbHandler.Unmount)
	// ====== 新增 ======
	mounts.GET("", kbHandler.ListMounts)
	mounts.GET("/:id", kbHandler.GetMount)
	mounts.PUT("/:id/permissions", kbHandler.UpdateMountPermissions)
	// =================
}

// User accessible KBs
authenticated.GET("/user/accessible-kbs", kbHandler.GetAccessibleKBs)
// ====== 新增 ======
authenticated.GET("/user/mounts", kbHandler.GetUserMounts)
// =================
```

---

## 验证方法

### 后端API测试

```bash
TOKEN="your_jwt_token"

# 1. 获取知识库的挂载列表
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/knowledge-bases/kb_xxx/mounts

# 2. 获取当前租户的所有挂载
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/mounts?mount_type=tenant

# 3. 获取单个挂载详情
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/mounts/1

# 4. 更新挂载权限
curl -X PUT -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"permissions": {"can_read": true, "can_write": false}}' \
  http://localhost:8080/api/v1/mounts/1/permissions

# 5. 获取当前用户的所有挂载
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:8080/api/v1/user/mounts
```

---

## 前端实现（可选 - 时间允许）

### 创建权限管理页面

**文件：** `projects/01-ai-knowledge-base-frontend/src/pages/MountManagement.tsx`（新建）

```typescript
import { useState } from 'react'
import { Shield, Users, User, Building } from 'lucide-react'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from '@/components/ui/table'

export const MountManagement: React.FC = () => {
  // 实现挂载管理UI
  return (
    <div className="space-y-6">
      <div>
        <h1 className="text-3xl font-bold tracking-tight">权限管理</h1>
        <p className="text-muted-foreground">管理知识库的访问权限和挂载关系</p>
      </div>

      <Card>
        <CardHeader>
          <CardTitle>挂载列表</CardTitle>
          <CardDescription>查看和管理知识库挂载关系</CardDescription>
        </CardHeader>
        <CardContent>
          {/* 挂载列表表格 */}
          <p className="text-center text-muted-foreground py-8">
            功能开发中...
          </p>
        </CardContent>
      </Card>
    </div>
  )
}
```

---

## 完成标准

- [ ] KBHandler中添加5个新方法
- [ ] KBService中添加6个新方法
- [ ] 路由注册完成
- [ ] 所有curl测试通过
- [ ] 代码通过编译
- [ ] Swagger文档生成正确
- [ ] 权限验证正常工作
- [ ] 错误处理完善
- [ ] 日志记录清晰

---

## 涉及文件清单

**修改文件：**
- `projects/01-ai-knowledge-base/internal/handler/kb_handler.go`
- `projects/01-ai-knowledge-base/internal/service/kb_service.go`
- `projects/01-ai-knowledge-base/cmd/server/main.go`

**相关文件（无需修改）：**
- `projects/01-ai-knowledge-base/internal/repository/mount_repository.go`

---

**任务完成后，权限管理体系将更加完善，为构建权限管理页面奠定基础。**

