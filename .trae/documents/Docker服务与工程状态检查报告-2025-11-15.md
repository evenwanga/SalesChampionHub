# Docker服务与工程状态检查报告（本地）

时间：2025-11-15

## 工程摘要

- 正在运行容器共 8 个，全部 Up (healthy) 且端口映射正常。
- 核心健康探针全部 2xx：前端、后端、嵌入服务、网关与认证服务工作正常。
- 工程结构与 Compose/Dockerfile 完整，所有必要组件（DB、缓存、应用、网关、认证）均已包含并统一接入共享网络。
- 数据库迁移已应用：`knowledge_base_mounts` 包含 `created_at/updated_at`；函数 `get_user_accessible_kbs` 存在；当前测试数据计数 KB=2、Documents=0、Chunks=0。

## 服务状态

- 运行容器（`docker ps`）
  - `kb-frontend` 映射 `3000->80`，healthy
  - `kb-api-server` 映射 `8080->8080`，healthy
  - `kb-bge-embedding` 映射 `8100->8000`，healthy
  - `saleschampion-redis` 映射 `6379->6379`，healthy
  - `user-center-custom-api` 映射 `3003`，healthy
  - `logto-core` 映射 `3001-3002`，healthy
  - `kong-gateway` 映射 `80/443/8001/8002/8444/8445`，healthy
  - `saleschampion-postgres` 映射 `5432->5432`，healthy

## 健康探针与接口验证

- `http://localhost:8080/health` → 200
- `http://localhost:8100/health` → 200
- `http://localhost:3000/env-config.js` → 200
- `http://localhost:8001/`（Kong Admin API）→ 200
- `http://localhost:3001/api/status`（Logto）→ 204

## 日志要点（近 200 行）

- `kb-api-server`：持续 `/health` 200；KB 列表与更新请求返回 200；RLS 上下文设置正常。
- `kb-bge-embedding`：频繁 `/health` 200，无错误告警。
- `kb-frontend`：运行时 `env-config.js` 已生成；少量未登录态触发 401 跳转 `/login`。
- `kong-gateway`：多次经网关的 KB 接口请求 200；偶发 401 与会话相关，符合未登录态行为。
- `logto-core`：`/api/status` 204；OIDC token 与 JWKS 请求正常。
- `user-center-custom-api`：数据库连接周期确认，`/health` 200。
- `saleschampion-redis`：周期性 RDB 保存成功，无错误。
- `saleschampion-postgres`：多次 `WARNING: SET LOCAL can only be used in transaction blocks`，建议在事务块内执行或调整设置方式。

## 工程完整性

- Compose 文件：
  - `infrastructure/docker-compose.yml`：`postgres`、`redis`、`kong-migrations`、`kong`、`kong-deck`
  - `projects/01-ai-knowledge-base/docker-compose.yml`：`kb-api-server`、`kb-bge-embedding`
  - `projects/01-ai-knowledge-base-frontend/docker-compose.yml`：`kb-frontend`
  - `projects/00-user-center/docker-compose.yml`：`logto-core`、`user-center-custom-api`
- Dockerfile：
  - `01-ai-knowledge-base-frontend/Dockerfile`
  - `01-ai-knowledge-base/services/bge-embedding/Dockerfile`
  - `01-ai-knowledge-base/Dockerfile`
  - `00-user-center/custom-api/Dockerfile`
- 网络：统一使用 `infrastructure_saleschampion_network`；跨容器通信验证正常。

## 进度分析（对照需求/设计）

- 用户中心（项目 0）：`api-specification.md`、`technical-design.md` 的认证与租户/权限模型符合当前部署与日志表现。
- AI 知识库（项目 1）：`知识库挂载策略设计.md` 与 `ARCHITECTURE_CHANGE.md` 中的挂载模型与访问函数已落地（函数存在；KB 测试数据可用）。
- 跨项目架构：`architecture-planning-discussion.md` 指定的依赖矩阵与中心化认证已按共享网络协作部署。
- 数据与检索：当前 `documents/document_chunks` 为 0，建议补充少量测试样例以覆盖检索与 RAG 联合路径。

## 现存问题

- ✅ ~~数据库会话设置警告：`SET LOCAL can only be used in transaction blocks` 频繁出现~~ **[已修复 2025-11-15]**
  - 中间件已改用 `SET` 命令，事务中保持使用 `SET LOCAL`
  - 详见：[问题修复报告-2025-11-15.md](./问题修复报告-2025-11-15.md)
- ✅ ~~测试数据缺口：`documents/document_chunks` 为空~~ **[已补充 2025-11-15]**
  - 已插入 3 个文档、9 个文档块、9 个向量
  - 脚本位置：`projects/01-ai-knowledge-base/scripts/load_test_data.sh`
  - 详见：[问题修复报告-2025-11-15.md](./问题修复报告-2025-11-15.md)

## 后续建议与优先级

- ✅ P0 修复 **[已完成 2025-11-15]**
  - ~~整理数据库会话设置：将所有 `SET LOCAL` 放入显式事务或改为 `SET` 并控制作用域，避免非事务上下文警告与行为隐患。~~
  - 已修复，详见 [问题修复报告](./问题修复报告-2025-11-15.md)
- P1 优化
  - 增加后端 API 集成测试（建议 `vitest + supertest` 或 Playwright API 模式），覆盖认证、权限与 KB 列表/更新；配置告警阈值关注 `kong-gateway` 与 `kb-bge-embedding` 的资源占用。
- ✅ P2 建议 **[部分完成 2025-11-15]**
  - ~~补充测试数据：导入少量 `documents` 与 `document_chunks`，验证检索与 RAG 联合查询路径。~~ **[已完成]**
  - 安全收敛：生产环境替换示例密码与密钥，避免真实凭据入仓；最小化端口暴露。**[待处理]**

## 命令与结果来源

- 容器与端口：`docker ps`
- 健康探针：`curl` 验证各服务端点
- 日志：`docker logs --tail 200 <container>`
- 工程结构：各子项目与基础设施 `docker-compose.yml`，相关 `Dockerfile`
- 数据库校验：`docker exec saleschampion-postgres psql` 查询列/函数与计数

---

## 更新记录

**2025-11-15 修复完成**
- ✅ P0 问题：数据库 SET LOCAL 警告已修复
- ✅ P2 问题：测试数据已补充（3个文档、9个块、9个向量）
- 📄 详细信息：[问题修复报告-2025-11-15.md](./问题修复报告-2025-11-15.md)

---

本报告仅记录状态与建议，不包含任何生产密钥或敏感配置值。

